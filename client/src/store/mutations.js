
export default {
    SAVE_USER_ID(state, user_id) {
        state.user_id = user_id;
      },
 
      USER_STATE(state , user_state){
        state.user_state = user_state;

      },
      USER_IMAGE(state ,  profile_image   ){
        state.profile_image = profile_image;

      },
      
} 